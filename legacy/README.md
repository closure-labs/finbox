# Pending removal after sandbox acceptance

These workflows are preserved for reference and are not discovered by GitHub
Actions. The old `bootc/`, `installer/`, `lib/ci-applications/` and Den image graph
remain available to compare runtime behavior until BlueBuild images and the
upstream ISO pass installation, upgrade and rollback tests. They do not control
the sandbox image pipeline. Do not re-enable their production publication jobs
in finbox.
